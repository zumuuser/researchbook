name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  publish-tauri:
    permissions:
      contents: write
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-action@stable

      - name: Install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: npm install

      - name: Build Tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'ResearchBook ${{ github.ref_name }}'
          releaseBody: |
            ## What's New
            - Excalidraw-powered spatial annotation canvas
            - Page-specific annotation layers with SQLite persistence
            - Shift+1 fit-to-screen shortcut
            - Toolbar hideout toggle
            - Dark mode support
            - PDF outline / table of contents navigation

            ## Installation
            - **Linux**: Download `.deb` or `.rpm`
            - **Windows**: Download `.msi` or `.exe`
            - **macOS**: Download `.dmg` (Intel or Apple Silicon)
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
